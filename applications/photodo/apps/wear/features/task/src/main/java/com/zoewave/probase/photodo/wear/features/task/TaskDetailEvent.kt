package com.zoewave.probase.photodo.wear.features.task

/**
 * User intents for the Task Detail screen.
 */
sealed interface TaskDetailEvent {
    // Note: View-Only mode means few events here for now.
    // In the future, "Toggle Task" would be here.
}
