# Detailed Code-to-Doc Audit & Verification

| Architectural Specification / Invariant    | Implementation File             | Verification Status                                                                                                                                                        |
| ------------------------------------------ | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Type-Safe Privacy Boundary (`AiInput`)** | `AiInput.kt`                    | **Matched 1:1** (`TextOnly` vs `Multimodal` sealed interface; cloud providers reject `Multimodal` at runtime).                                                             |
| **Selection Tier Hierarchy**               | `StyleModels.kt`                | **Matched 1:1** (`SelectionTier.SELECTED`, `LOCKED`, `FORCED` and `UserConstraint` data classes).                                                                          |
| **Chroma-Weighted $L^*C^h$ Color Science** | `ColorHarmonyEngine.kt`         | **Matched 1:1** (Pure-Kotlin $L^*C^h$ conversion, vector addition $\Sigma (C^ \times \cos(h^\circ))$, low-chroma neutral handling, $\Delta E_{00}$).                       |
| **Role Gap Analysis**                      | `RoleGapAnalyzer.kt`            | **Matched 1:1** (Evaluates `RoleRequirement` min/max counts for Tops, Bottoms, Shoes, Outerwear based on occasion & temperature).                                          |
| **Signature Item Rotation Bypass**         | `RotationScoringUseCase.kt`     | **Matched 1:1** (`isSignature == true` immediately returns `0.0f` penalty; 3-day window for clothes, 24h for cosmetics).                                                   |
| **Deterministic Context & State Pipeline** | `DeterministicContextEngine.kt` | **Matched 1:1** (`generateSelectionState` resolves anchors, calculates composite profile, executes hard gating, applies soft $L^*C^h$ scoring, returns full ranked pool).  |
| **Role-Allocated Preflight Step-Down**     | `StyleSimulatorEngine.kt`       | **Matched 1:1** (Audits exact `AiInput` token count; steps down `EXPANDED` $\to$ `BALANCED` $\to$ `MINIMAL` $\to$ $K - 2$ on candidate additions only; preserves anchors). |
| **Post-Retrieval Deterministic Cache Key** | `StyleSimulatorEngine.kt`       | **Matched 1:1** (SHA-256 fingerprint embeds post-retrieval state: Selected IDs, `missingRoleRequirements`, occasion, `weatherTempC`, `uvIndex`, telemetry, `providerId`).  |
| **Audit & Transparency Logger**            | `StyleAuditLogger.kt`           | **Matched 1:1** (Pretty-prints 5-stage timeline under Logcat tag `KoColor_Audit`).                                                                                         |
| **Greedy Rehydration & Anchor Fail-Safe**  | `GreedyRehydrator.kt`           | **Matched 1:1** (Dual-domain anchor fail-safe for clothing + cosmetics, keyword fallbacks for `ACTIVEWEAR`, `"No Item Left Behind"` slotting).                             |
| **Interactive Selection ViewModel State**  | `StyleSimulatorViewModel.kt`    | **Matched 1:1** (Maps family chips and explicit locks to `UserConstraint` lists, 300ms state debouncing, unidirectional flow).                                             |
