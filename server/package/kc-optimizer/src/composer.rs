use crate::models::KpssSource;
use std::collections::HashMap;

pub fn build_packages(_canonical_index: &HashMap<String, KpssSource>, _package_configs_dir: &str) {
    // TODO: Implementation for resolving TOML and building .kpkg
}
